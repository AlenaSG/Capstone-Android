<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.epicodus.eventmanager">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignInEmailActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".CreateEventActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".FindEventActivity" />
        <activity android:name=".AddRatingActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".EventDetailActivity" />
        <activity android:name=".WelcomeScreenActivity" />
        <activity android:name=".TodayEventsActivity" />
        <activity android:name=".util.AllFutureEventsActivity"></activity>
    </application>

</manifest>